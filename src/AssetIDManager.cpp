#include<hgl/asset/AssetIDManager.h>
#include<chrono>

namespace Asset
{
    using namespace hgl;

    TimeID GetCurrentTimeID()
    {
        TimeID id{};

        return id;
    }

    uint32 GetLocalDeveloperID()
    {
        return 0;
    }
}//namespace Asset
